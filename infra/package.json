{
    "name": "infra-stardust",
    "main": "index.ts",
    "devDependencies": {
        "@types/node": "^16"
    },
    "scripts": {
        "up": "pulumi up --stack dev -y",
        "logs": "pulumi logs --stack dev -f",
        "down": "pulumi destroy --stack dev -y",
        "cleanup": "pulumi stack rm dev -y",
        "preview": "pulumi preview --stack dev"
    },
    "dependencies": {
        "@pulumi/aws": "^6.0.0",
        "@pulumi/awsx": "^2.0.2",
        "@pulumi/cloudflare": "^5.22.0",
        "@pulumi/command": "^0.9.2",
        "@pulumi/pulumi": "^3.0.0",
        "@t3-oss/env-nextjs": "^0.6.0",
        "dotenv": "^16.3.1",
        "zod": "^3.21.4"
    }
}