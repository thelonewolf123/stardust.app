{
    "name": "infra-soul-forge",
    "main": "index.ts",
    "devDependencies": {
        "@types/node": "^16"
    },
    "scripts": {
        "infra:up": "pulumi up --stack dev -y",
        "infra:down": "pulumi destroy --stack dev -y",
        "infra:preview": "pulumi preview --stack dev",
        "infra:cleanup": "pulumi stack rm dev -y",
        "infra:logs": "pulumi logs --stack dev -f"
    },
    "dependencies": {
        "@pulumi/aws": "^5.0.0",
        "@pulumi/awsx": "^1.0.5",
        "@pulumi/pulumi": "^3.0.0",
        "@t3-oss/env-nextjs": "^0.6.0",
        "dotenv": "^16.3.1",
        "zod": "^3.21.4"
    }
}