query GetDeploymentHistory($slug: String!) {
    getProjectBySlug(slug: $slug) {
        history {
            containerSlug
            createdAt
            status
        }
        current {
            containerSlug
            status
        }
        slug
        name
    }
}